# * Add a "top" field to Dependency, of type Dependency
# * When "going up" a -% link, append the corresponding Dependency. 
# * In traces code, output all extra dependencies. 

# Check that the error message is correct
A: [[[B]]] { touch A ; }
>B { echo C }
>C { echo D }
>D { echo E }
E: X { touch E }
