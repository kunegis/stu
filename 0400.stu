# make Execution::parent have vector <shared_ptr <const Dependency> > as
# second type arg. 

# Check that the error message is correct
A: [[[B]]] { touch A ; }
>B { echo C }
>C { echo D }
>D { echo E }
E: X { touch E }
