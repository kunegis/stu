A: list.directory list.directory/B
{
	cp list.directory/B A
}

-p list.directory { mkdir -p list.directory ; }

%cd list.directory

$dir/B
{
	pwd
	printf 'dir="%s"\n' "$dir"
	mkdir -p -- "$dir"
	echo correct >"$dir"/B
}
