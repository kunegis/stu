# Because @x is a "side-phony" and not a transitive phony, it does not
# act transitively.  Instead, this is an error:  non-transitive
# phonies cannot be used in dynamic dependencies. 

A: [@x] { cp D A ; }
B @x: C { echo bbb >B ; }
C { echo D >C ; }
D { echo correct >D ; }
