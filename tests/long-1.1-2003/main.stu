# Interrupt Stu immediately after startup with a SIGINT. 

A:  [list.dep] 
{ touch A }

>list.dep: {
	../../sh/seq 999 | sed -e 's,^,list.,'
}

list.$n
{
	touch "list.tag.$n"
	sleep 74634275
	touch "list.$n"
}
