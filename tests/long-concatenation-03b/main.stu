A:  [B] {
	N=5
	[ -e list."$N" ]       || exit 1
	[ -e list."$((N+1))" ] && exit 1
	exit 0
}

>B:
{
	for i in $(seq 1 5) ; do
		printf '%s' '['
	done
	printf list.0
	for i in $(seq 1 5) ; do
		printf '%s' ']'
	done
	echo
}

>list.$n:
{
	N=5
	if [ "$n" = "$((N - 1))" ] ; then
		echo X
	else
		echo list."$((n + 1))"
	fi
}
